import 'package:flutter/material.dart';

class NavScreenMapbox extends StatelessWidget {
  const NavScreenMapbox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Placeholder in case mapbox is implemented'),
    );
  }
}
